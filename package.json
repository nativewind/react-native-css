{
  "name": "react-native-css-monorepo",
  "version": "0.0.0",
  "description": "",
  "private": "true",
  "scripts": {
    "test": "npm run test --ws --",
    "website": "npm run start --workspace=website --",
    "build": "npm run build --ws --",
    "clean": "find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +; npm ci"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.4.0",
    "@react-native-community/cli": "^15.1.2",
    "@tsconfig/react-native": "^3.0.5",
    "docusaurus-plugin-typedoc": "^1.1.0",
    "eslint": "^9.15.0",
    "prettier": "^3.4.1",
    "typedoc": "^0.27.2",
    "typedoc-plugin-markdown": "^4.3.1",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@testing-library/react-native": "^12.8.1"
  }
}
